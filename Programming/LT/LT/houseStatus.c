#include "houseStatus.h"

// You can define functions related to HomeStatus here if needed

// For example, if you want to initialize the struct with default values:
/*
void initializeHomeStatus(HomeStatus *status) {
    status->window1 = 0;
    status->window2 = 0;
    status->door = 0;
    status->lock = 0;
    status->light = 0;
    status->keyboardChar = '\0';
}
*/